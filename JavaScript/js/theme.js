/**
 * First way of definig property and method
 * you can define any property or method and can use anywhere directly after document ready
 */

const primaryColor = '#388E3C';
const primaryTextColor = '#FFFFFF';

/**
 * can put hardcoaded in html but here we returning title from js file just to the implementation
 */ 
const getPersonFormTitleText = () => {
    return "Add / Update Person";
}

/**
 * can put hardcoaded in html but here we returning title from js file just to the implementation
 */ 
function getPersonListTitleText(){
    return "Saved Persons";
}